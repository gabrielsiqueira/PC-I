#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
int main()
{
  int x,y;
  x = 1;
  while(x < 11)
  {
    if(x < 4)
    {
      printf(Y == 1);
    }
    else
    {
      if(x == 4)
      {
        printf("Y = 32");
      }
      else
      {
        if(x > 4)
        {
          y = pow(x,2) - 16;
          printf("Y = %g",y);
        }
      }
      x = x + 1;
    }
  }
